#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>
#include <unistd.h>

int _strlen(char *s);
void _puts(char *str);
int _atoi(char *s);
char *_strcpy(char *dest, char *src);
int _putchar(char c);

#endif
